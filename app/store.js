// DS.RESTAdapter.reopen({
//   namespace: 'api'
// });

// DS.RESTAdapter.map('App.FireCall', {
//   location: { embedded: 'always' },
//   address: { embedded: 'always' }
// });

// App.ApplicationAdapter = DS.RESTAdapter();

//namespace: 'api